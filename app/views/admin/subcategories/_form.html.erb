<%= simple_form_for([:admin, @subcategory]) do |f| %>
  <%= f.input :name, label: "Nom de la sous-catégorie", placeholder: "Pensez à indiquer 'AB'" %>
  <%= f.input :order, label: "Ordre d'apparition dans la liste" %>
  <%= f.hidden_field :category_id, value: category.id %>
  <%= f.button :submit, "Valider", class: "btn btn-primary" %>
<% end %>

