<div class="container">
  <h1>Récolte par légume - Général</h1>
  <p><%= link_to "Retour", :back %></p>

  <ul>
    <% @orders_by_year.each do |date, orders_number| %>
      <li>
        <p>
          <%= date.strftime('%Y') %> : <%= pluralize(orders_number, "commande") %> -
          <%= link_to "Détail", admin_harvest_path(date.strftime('%Y')) %>
        </p>
      </li>
    <% end %>
  </ul>
</div>
