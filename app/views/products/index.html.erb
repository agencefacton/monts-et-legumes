<div class="container">
  <div class="card_form">
    <h1>Les nouvelles de la semaine</h1>
    <% if @post.content? %>
      <p><%= @post.content %></p>
    <% end %>
  </div>

  <div id="customer_list_cart">
    <div class="row">
      <div class="col-8">
        <div id="products-list">
          <h1>Sélectionnez vos produits</h1>
          <%= render 'product_list' %>
        </div>
      </div>

      <div class="col-4">
        <h1><i class="fas fa-shopping-basket"></i> Votre panier</h1>
        <div class="card_form" id="order-cart">
          <%= render 'cart' %>
          <%= render 'cart_validation' %>
        </div>
      </div>
    </div>
  </div>
</div>
