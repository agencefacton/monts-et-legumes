<div class="container">
  <div class="card_form">
    <h1>Vous avez oublié votre mot de passe ?</h1>
    <p>Indiquez votre adresse mail et nous vous enverrons les instructions pour le réinitialiser.</p>



    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= devise_error_messages! %>

        <%= f.input :email, autofocus: true, autocomplete: "email", label: "Email" %>
        <%= f.submit "Recevoir les instructions par email", class: "btn btn-primary btn-space" %>

    <% end %>

    <%= render "devise/shared/links" %>

  </div>


</div>
