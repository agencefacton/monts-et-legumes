<div class="container">
  <h1>Vous avez oublié votre mot de passe ?</h1>
  <p>Ne vous en faites pas, ça arrive même aux meilleurs ! </br> Indiquez votre adresse mail et nous vous enverrons les instructions pour le réinitialiser.</p>



<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

    <%= f.input :email, autofocus: true, autocomplete: "email" %>
    <%= f.submit "Recevoir les instructions par email", class: "btn btn-primary" %>

<% end %>

<%= render "devise/shared/links" %>


</div>
