<div class="container">
  <h1>A récolter - Semaine <%= @week %></h1>
  <ul>
    <% @quantities.each do |quantity| %>
    <li><%= quantity %> </li>
  <% end %>
  </ul>

<%= @quantities3 %>

  <%= link_to "Exporter", product_orders_path(format: "xlsx") %>

</div>
