<div class="container">

  <h1>Récolte par légume - <%= @year %>, semaine <%= @week %></h1>
  <p><%= link_to "Retour", product_orders_path %></p>

  <ul>

  <% @quantities.each do |quantity| %>
    <li><%= quantity[0].category %> : <%= quantity[0].name %> - <%= pluralize(quantity[1], quantity[0].unit) %></li>
  <% end %>

  </ul>
</div>
