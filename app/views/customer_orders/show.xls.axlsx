wb = xlsx_package.workbook

wb.add_worksheet(name: "Commandes client") do |sheet|
  sheet.add_row %w(Nom Prénom)

  @quantities.each do |product_order|
    sheet.add_row [product_order.first[1], product_order[1]]
  end
end
